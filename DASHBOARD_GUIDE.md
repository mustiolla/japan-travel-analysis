# ✈️ 원/엔 환율 & 방일 여행객 시계열 대시보드 사용자 가이드

본 문서는 **보너스 과제(분석 결과 서비스화 및 시계열 심화 옵션)** 제출 요건을 충족하기 위한 종합 안내서입니다.  
본 가이드는 **[제출 옵션 2: 로컬 실행 방법 문서]** 및 **[제출 옵션 3: 대시보드 구성 + 필터/기간 변경 시나리오 설명]**을 모두 포함하고 있습니다.

---

## 1. 개요 및 대시보드 소개

- **서비스 명칭**: 방일 한국인 여행객 수와 원/엔 환율 시계열 인터랙티브 탐색기 (`app.py`)
- **기술 스택**: `Streamlit` (UI 및 반응형 프레임워크), `Plotly` (인터랙티브 시각화), `Statsmodels` (통계 회귀, 시계열 분해, Holt-Winters 예측), `Pandas`
- **주요 가치**: 사용자가 정적인 보고서에 머무르지 않고, **직접 기간(Date Range)과 조건(Lag, 역사적 사건 오버레이, 이동평균선)**을 실시간으로 변경해가며 경제 원리와 데이터의 숨은 패턴을 탐색할 수 있습니다.

---

## 2. 로컬 실행 방법 (제출 옵션 2 요건)

### 2.1 사전 환경 준비
- Python 3.10 이상 권장
- 터미널(PowerShell 또는 bash)을 열고 프로젝트 루트 폴더로 이동합니다.

```bash
cd "d:\LEH\AI 네이티브\3. AI 응용학습\M1-1\japan-travel-analysis"
```

### 2.2 필수 패키지 설치
`requirements.txt`에 포함된 모든 라이브러리를 설치합니다.

```bash
pip install -r requirements.txt
```
*(이미 `streamlit`, `plotly`, `statsmodels`, `pandas` 등이 설치되어 있다면 생략 가능합니다.)*

### 2.3 대시보드 실행 명령어
아래 명령어를 터미널에 입력하여 Streamlit 서버를 구동합니다.

```bash
streamlit run app.py
```

- 명령어 실행 시 기본 웹 브라우저가 자동으로 열리며 대시보드 화면이 표시됩니다.
- 자동 실행되지 않을 경우 브라우저 주소창에 다음 주소를 입력합니다:  
  👉 **`http://localhost:8501`**

---

## 3. 대시보드 핵심 기능 및 인터페이스 구성

### 3.1 🧭 좌측 사이드바 (조건 제어판)
1. **분석 기간 프리셋 (Radio)**:
   - `전체 기간 (2010~현재)`: 15년간의 전체 장기 시계열 탐색
   - `코로나 이전 황금기 (2010~2019)`: 외교/감염병 왜곡이 배제된 순수 경제 원리(환율 효과) 분석
   - `코로나 충격 및 회복기 (2020~현재)`: 팬데믹 충격 및 포스트 코로나 역대급 엔저 붐 분석
   - `사용자 직접 지정`: 날짜 슬라이더로 원하는 연월을 자유롭게 설정
2. **환율 시차(Lag) 반영 슬라이더 (0~6개월)**:
   - 슬라이더를 움직이면 메인 화면의 **상관계수($r$), p-value, 회귀 결정계수($R^2$), 산점도**가 즉각 반응하여 재계산됩니다.
3. **6대 역사적 충격(Black Swan) 오버레이 토글**:
   - 동일본 대지진, 세월호, 메르스, 구마모토 지진, 노재팬, 코로나19 등 역사적 충격 지점을 시계열 차트 위에 수직선과 텍스트로 표시합니다.
4. **이동평균선 (3MA / 12MA) 토글**:
   - 단기 계절 노이즈를 완화하고 중장기 트렌드를 명확히 관찰할 수 있습니다.

### 3.2 📊 상단 실시간 KPI 카드
선택한 기간 및 시차 조건에 따라 다음 지표가 실시간으로 갱신됩니다:
- **총 누적 방문객** / **월평균 방문객** / **평균 원/엔 환율** / **상관계수(r & p-value)** / **회귀 결정계수($R^2$ & 기울기)**

---

## 4. 기간/조건 변경 탐색 시나리오 (제출 옵션 3 요건)

### 📌 시나리오 1: 순수 경제 원리 검증 (코로나 이전: 2010~2019)
- **설정 조건**:
  - 기간: `코로나 이전 황금기 (2010~2019)`
  - 환율 시차(Lag): `3개월`
- **탐색 결과 및 해석**:
  - 상관계수가 **$r = -0.589$ ($p < 0.001$)**로 매우 강력한 음의 상관관계를 나타냄.
  - 환율이 100엔당 100원 하락할 때 3개월 후 방일객이 약 **7.7만 명 증가**하는 선명한 선형 패턴 확인.
  - 소비자가 엔저를 체감하고 항공권/호텔을 예약하여 실제 출국하기까지 **3개월의 예약 지연(Booking Lead-time)**이 발생함을 입증.

### 📌 시나리오 2: 환율 시차(Lag) 0개월 vs 3개월 비교 탐색
- **설정 조건**:
  - 기간: `2010~2019` 고정
  - 시차 변경: `Lag 0개월` ➡️ `Lag 3개월` ➡️ `Lag 6개월`
- **탐색 결과 및 해석**:
  - `Lag 0개월`: $r = -0.407$
  - `Lag 1개월`: $r = -0.479$
  - `Lag 2개월`: $r = -0.548$
  - `Lag 3개월`: **$r = -0.589$ (피크/골든타임)**
  - `Lag 6개월`: $r = -0.485$ (영향력 점차 감쇄)
  - 대시보드 탭 2의 **"시차별 상관계수 비교 막대그래프"**에서 3개월 시차의 피크가 시각적으로 명확하게 드러남.

### 📌 시나리오 3: 6대 역사적 충격(Black Swan)의 경제 원리 압도 관찰
- **설정 조건**:
  - 기간: `전체 기간 (2010~현재)`
  - 6대 역사적 충격 오버레이: `체크(ON)`
- **탐색 결과 및 해석**:
  - 2011년 동일본 대지진(방일객 -64.2%), 2019년 노재팬 불매운동(-65.1%), 2020년 코로나19(-99.9%) 시점에 수직선이 표기되며, 환율이 유리한 상태임에도 불구하고 방문객 수가 수직 낙하하는 현상을 목격.
  - 12개월 이동 상관계수(Rolling Correlation)가 정상적인 음수(-0.8)에서 일시에 양수(+0.7)로 역전되는 **'구조적 단절'**을 직관적으로 확인 가능.

### 📌 시나리오 4: 시계열 베이스라인 단기 예측 시뮬레이션 (심화 옵션 B)
- **설정 조건**:
  - 대시보드 `탭 4: 베이스라인 단기 예측` 선택
  - 학습 구간: `최근 회복기 중심 (2022.06~현재)`
  - 예측 기간: `향후 6개월`
- **탐색 결과 및 해석**:
  - Holt-Winters 지수평활법 모델이 최근의 엔저 회복 추세(Trend)와 12개월 주기 계절성(Seasonality)을 반영하여 향후 월별 예상 방문객 수 및 80%/95% 신뢰구간을 산출.
  - **전제 가정 (Assumptions)**:
    1. 1월(방학), 7~8월(여름휴가), 10월(단풍), 12월(연말)의 고정된 계절 주기성이 미래에도 유지됨.
    2. 엔저 기조 및 거시경제 환경이 급격한 변동 없이 연착륙함.
  - **구조적 한계 (Limitations)**:
    1. **블랙스완 무감지**: 신종 감염병, 지정학적 충돌 등 외생적 충격 발생 시 통계 모델은 즉시 예측 실패.
    2. **물리적 공급 상한 미반영**: LCC 항공기 좌석 공급 한계 및 일본 내 호텔/인프라 오버투어리즘 제약을 모델이 인지하지 못함.
    3. **비선형적 탄력성 왜곡**: 환율이 900원대 중후반으로 급등하거나 800원선이 붕괴될 때의 소비자 심리 쏠림을 선형/가법 모형으로 완전 포착 불가.

---

## 5. 결론 및 기대 효과

본 웹 대시보드를 통해 심사자 및 사용자는:
1. 복잡한 파이썬 코드를 실행하지 않고도 웹 브라우저에서 슬라이더 조작만으로 가설을 실시간 검증할 수 있습니다.
2. 환율과 여행 수요 간의 **"3개월 시차 골든타임"**, **"계절성의 분산 기여"**, **"블랙스완의 파괴력"**을 손쉽게 시각적으로 체감할 수 있습니다.
3. 시계열 예측 결과와 함께 **통계 모델의 가정 및 한계**를 균형 있게 이해할 수 있습니다.
